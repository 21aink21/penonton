<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false">

    <!-- 1. Layer Belakang (Layer Duplikat: Blur 30%, Opacity 70% Seamless Ambient Glow) -->
    <com.donghuaz.ui.widget.FadingImageView
        android:id="@+id/ivBannerBlur"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:alpha="0.70"
        android:importantForAccessibility="no"
        android:scaleType="centerCrop"
        android:scaleX="1.08"
        android:scaleY="1.08" />

    <!-- 2. Layer Depan (Foreground Crisp Poster with Seamless Hardware Fade) -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Crisp Main Poster Image with 100% Seamless Bottom Fade -->
        <com.donghuaz.ui.widget.FadingImageView
            android:id="@+id/ivBanner"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop" />

        <!-- Bottom Gradient Overlay for Text Readability -->
        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_gradient_overlay" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:orientation="vertical"
            android:paddingHorizontal="16dp"
            android:paddingBottom="24dp">

            <TextView
                android:id="@+id/tvBannerTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/text_primary"
                android:textSize="18sp"
                android:textStyle="bold"
                android:shadowColor="#CC000000"
                android:shadowDx="0"
                android:shadowDy="2"
                android:shadowRadius="6"
                tools:text="Soul Land 2 The Peerless Tang Clan" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <!-- Top Rank Badge -->
                <TextView
                    android:id="@+id/tvBannerRank"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:background="@drawable/bg_badge_gold"
                    android:paddingHorizontal="8dp"
                    android:paddingVertical="3dp"
                    android:textColor="@color/background"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    tools:text="🔥 Top #1" />

                <!-- Episode Badge (Gold Accent) -->
                <TextView
                    android:id="@+id/tvBannerEp"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="8dp"
                    android:background="@drawable/bg_badge_gold_dark"
                    android:paddingHorizontal="8dp"
                    android:paddingVertical="3dp"
                    android:textColor="@color/accent_gold"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    tools:text="EP 168" />

                <!-- Rating Badge (Gold Accent) -->
                <TextView
                    android:id="@+id/tvBannerRating"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="8dp"
                    android:background="@drawable/bg_badge_gold_dark"
                    android:paddingHorizontal="8dp"
                    android:paddingVertical="3dp"
                    android:textColor="@color/accent_gold"
                    android:textSize="13sp"
                    android:textStyle="bold"
                    tools:text="★ 9.5" />
            </LinearLayout>
        </LinearLayout>
    </FrameLayout>
</FrameLayout>
